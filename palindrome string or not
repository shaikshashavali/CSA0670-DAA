#include <stdio.h>
#include <string.h>
 
int main()
{
    char s[1000];  
    int i,n,k=0;
 
    printf("Enter  the string : ");
    gets(s);
    n=strlen(s);
 
    for(i=0;i<n/2;i++)  
    {
    	if(s[i]==s[n-i-1])
    	k++;
 
 	}
 	if(k==i)
 	    printf("string is palindrome");
    else
        printf("string is not palindrome");
 
 	 
     
    return 0;
}
